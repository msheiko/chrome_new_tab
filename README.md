# Custom Chrome New Tab Extension

Кастомная страница новой вкладки для Google Chrome с переключением между домашними и рабочими сайтами.

## 🚀 Особенности

- ✨ Современный интерфейс на Vue 3 с Composition API
- 🔄 Переключение между режимами "Home" и "Work"
- 💾 Сохранение выбранного режима в localStorage
- 🎨 Адаптивный дизайн с карточками сайтов
- ⚡ Быстрая сборка с Vite
- 🎯 Оптимизированная production сборка

## 📋 Требования

- Node.js 18+ 
- npm 9+

## 🛠️ Установка

1. Клонируйте репозиторий или скачайте проект
2. Установите зависимости:

```bash
npm install
```

## 🏃 Разработка

Запуск dev-сервера для разработки:

```bash
npm run dev
```

Сервер будет доступен по адресу `http://localhost:3000`

## 📦 Сборка

Создание production сборки:

```bash
npm run build
```

Собранные файлы будут находиться в папке `dist/`

## 🔍 Просмотр сборки

Для просмотра production сборки локально:

```bash
npm run preview
```

## 🧹 Линтинг

Проверка и автоматическое исправление кода:

```bash
npm run lint
```

## 📁 Структура проекта

```
chrome_new_tab/
├── public/              # Статические файлы
│   ├── favicon.ico
│   ├── img/            # Изображения
│   └── manifest.json   # Манифест расширения
├── src/                # Исходный код
│   ├── App.vue         # Главный компонент
│   ├── SiteItem.vue    # Компонент карточки сайта
│   ├── data.json       # Данные сайтов
│   └── main.js         # Точка входа
├── dist/               # Собранные файлы (создается при сборке)
├── index.html          # HTML шаблон
├── vite.config.js      # Конфигурация Vite
├── eslint.config.js    # Конфигурация ESLint
└── package.json        # Зависимости и скрипты
```

## 🔧 Настройка сайтов

Сайты настраиваются в файле `src/data.json`. Каждый сайт имеет следующую структуру:

```json
{
  "url": "https://example.com",
  "image": "https://example.com/logo.png",
  "title": "Example",
  "home": true  // true для домашних, false для рабочих
}
```

### Поля:
- `url` - URL сайта
- `image` - URL изображения/логотипа сайта
- `title` - Название сайта
- `home` - `true` для домашних сайтов, `false` для рабочих

## 📦 Установка расширения в Chrome

1. Соберите проект:
   ```bash
   npm run build
   ```

2. Откройте Chrome и перейдите в `chrome://extensions/`

3. Включите "Режим разработчика" (Developer mode)

4. Нажмите "Загрузить распакованное расширение" (Load unpacked)

5. Выберите папку `dist` из проекта

6. Откройте новую вкладку - должна появиться ваша кастомная страница

## 🎨 Использование

- **Переключение режимов**: Используйте переключатель в правом верхнем углу для переключения между "Home" и "Work"
- **Выбор сайта**: Кликните на карточку сайта для перехода на него
- **Сохранение**: Выбранный режим автоматически сохраняется и будет использоваться при следующем открытии новой вкладки

## 🛠️ Технологии

- **Vue 3** - Прогрессивный JavaScript фреймворк
- **Vite** - Быстрый сборщик и dev-сервер
- **Composition API** - Современный подход к написанию компонентов Vue
- **@vueform/toggle** - Компонент переключателя
- **ESLint** - Линтер для проверки кода

## 📝 Скрипты

- `npm run dev` - Запуск dev-сервера
- `npm run build` - Сборка для production
- `npm run preview` - Просмотр production сборки
- `npm run lint` - Проверка и исправление кода

## 🔒 Разрешения расширения

Расширение требует следующие разрешения:
- `bookmarks` - Доступ к закладкам (зарезервировано для будущего использования)
- `<all_urls>` - Доступ ко всем URL для корректной работы ссылок

## 📄 Лицензия

Приватный проект

## 👤 Автор

Проект создан для персонального использования

---

**Примечание**: Это расширение для личного использования. При необходимости вы можете настроить список сайтов в `src/data.json` под свои нужды.
